var assert = require('assert')
  , tests
  , TempAuction = geddy.model.TempAuction;

tests = {
  'test stub, replace with your own passing test': function () {
    assert.equal(true, false);
  }

};

module.exports = tests;
